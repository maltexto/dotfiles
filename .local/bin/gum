#!/bin/sh

screenshot_dir="$HOME/pics/screenshots"
mkdir -p "$screenshot_dir"
shotgun -g "$(slop -f "%g")" "$screenshot_dir/screenshot-$(date +%Y%m%d-%H%M%S).png" && notify-send "screenshot saved" "$screenshot_dir"
